# Используем базовый образ GCC
FROM gcc:11

# Устанавливаем рабочую директорию внутри контейнера
WORKDIR /app

# Копируем исходный код
COPY . .

# Сборка программы
RUN g++ -o app main.cpp

# Команда по умолчанию для запуска
CMD ["./app"]
